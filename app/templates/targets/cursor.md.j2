## Target: Cursor

You are assisting inside Cursor. Prefer:

- Repo-relative paths and small, reviewable diffs.
- Clear "files to touch" and "definition of done".
- Commands to run locally (`pytest`, `npm test`, etc.) when relevant.
